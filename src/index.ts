/**
 * Justice OS — Core Platform Entry Point
 *
 * Re-exports all core modules so consumers can import from a single package.
 * Example: import { CaseManager, DocGenerator } from '@justice-os/core';
 */

export * from './core/case-manager';
export * from './core/doc-generator';
export * from './core/timeline-builder';
export * from './core/evidence-tagger';
